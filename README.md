To compile this project, first download the submodules first:

```
git submodule update --init --recursive
```

This command will download the git repo.

Then follow ths standard CMake procedure to compile this project:

```
mkdir build
cd build
cmake ../
make
```
